# mwanachama-git — completed tasks

| Task | Title | Completed | Notes |
|------|-------|-----------|-------|
| G1 | Bootstrap repo: `git init`, `go.mod`, `.gitignore`, `Makefile`, four-phase `documentation/` skeleton, `todo.md`/`todo_done.md` | 2026-09-02 | New repo. Depends on [mwanachama-go-shared](../../../mwanachama-go-shared) for its entity-graph store (wired via `go.mod` local `replace`). |
| G2 | Port `git.go`'s `GitManager` interface + `models.go` domain types (Repository/Branch/Commit/Tree/Blob/MergeRequest/Tag/Keyword) unchanged | 2026-09-02 | Added `models.go` (domain + request/filter types), `types.go` (`FileEntry`/`CommitEntry`/`FileDiff`/`ErrMergeConflict`/`ImportRepoRequest`/`ImportJob` — these are referenced directly by `GitManager`'s method signatures, so folded into this task even though `types.go` wasn't itself named on the board), and `errors.go` (v2 sentinel errors only — dropped the v1-only `ErrRepoNotFound`). Package name `mwanachamagit`. Dropped the unused v1 `AuthorInfo` type (dead even in the source). `git.go` carries the `GitManager` interface plus the self-contained `BlobSearcher`/`RefLocker`/`mutexLocker` support types; doc comments were lightly reworded (`gRPC server handler` → `HTTP handler`, `CodeValdWork`→`mwanachama-taskmanager`) since those referenced dropped v1 infrastructure. **Deferred to G4**: the concrete `gitManager` struct and `NewGitManager` constructor — the original wires in `entitygraph.DataManager` (ready), `Backend` (v1, out of scope), and `eventbus.Publisher` (not yet ported — `mwanachama-go-shared`'s `Publisher`/S7 is still in flight in another session as of this writing), so building the struct now would mean inventing a placeholder for a seam another repo owns. `go build ./...`, `go vet ./...`, and `gofmt -l .` all clean. |
| G3 | Port `schema.go`'s `DefaultGitSchema()` onto `mwanachama-go-shared`'s type defs | 2026-09-02 | `mwanachama-go-shared`'s S2 landed (committed) by the time this started, so the dependency was live. Mechanical port of all ten `TypeDefinition`s onto `schema.TypeDefinition`/`PropertyDefinition`/`RelationshipDefinition` — dropped `PathSegment`/`EntityIDParam` on every type and relationship (go-shared's route-generation fields don't exist; mwanachama-api-gateway registers HTTP routes by hand). Dropped the `GitInternalState` type entirely and `Repository.head_ref` — both exist solely for the v1 go-git `storage.Storer` (grepped the original: neither is read/written by any `git_impl_*.go` file), which CLAUDE.md already scoped out. Kept `bare_clone_path`/`fetched_commit_shas`/`source_url` on Repository, `status`/`source_url` on Branch, and the `ImportJob`/`FetchBranchJob` types — these back the lazy-import v2 methods G2 already kept on `GitManager`; G6 will decide whether the fetch business logic actually gets implemented, but the schema needs to exist regardless since `types.go`'s `ImportJob`/`FetchBranchJob` already promise it. Added `schema_test.go` asserting `entitygraph.ValidateSchema(DefaultGitSchema())` passes (catches dangling `ToType`s, missing inverse relationships, bad `UniqueKey` fields) — not a G9 port-fidelity test, just a cheap authoring guard for this file. `go build`, `go vet`, `go test`, `gofmt -l` all clean. |
| G8 | Wire `events.go`'s topic list to `mwanachama-go-shared`'s `Publisher` interface | 2026-09-02 | Did this out of dependency order relative to G4–G7 because `mwanachama-go-shared`'s S7 (`events.Publisher`) landed (on disk, in a parallel session) while G4 was still blocked on S4. Ported the outbound `Topic*` constants + `AllTopics()` + their payload structs (`RepoCreatedPayload` … `FileWrittenPayload`) into `events.go`, replacing the dropped `eventbus.DomainGit` import with a local `topicPrefix = "git."` const. **Dropped** `ConsumedTopics()`, `TopicBranchCreate`, `TopicFileWrite`, `BranchCreatePayload`(+`Resolve`), `FileWritePayload`(+`Resolve`), and `FileWriteKeyword` — grepped the original and confirmed none of `git_impl_*.go` reads them; they existed only so CodeValdGit could subscribe to a CodeValdCortex pub/sub bus and act on inbound LLM-emitted actions, and mwanachama-api-gateway calls `GitManager` methods directly from its own HTTP handlers instead, so there's no bus to subscribe to. **Did not** add the concrete `gitManager` struct / `NewGitManager` back despite `events.Publisher` now existing: `NewGitManager` returns the `GitManager` interface, and `*gitManager` won't satisfy that interface until enough of G4/G5/G6's methods are actually implemented — wiring the constructor now would just fail to compile. That piece still lands with G4. `go build`, `go vet`, `go test`, `gofmt -l` all clean; no new import needed since this file only references `events.Publisher` in doc comments. |
| G4 | Port pure-entitygraph impl files as-is: branch, converters, edgelifecycle, graph, keywords, mergerequests, repo, rollback, tag | 2026-09-02 | Started ahead of `mwanachama-go-shared`'s S4 landing — user call, since `git_impl_*.go` only ever calls the already-stable `entitygraph.DataManager` interface (S2), never Postgres directly; real Postgres integration testing still waits for G9/S4 as planned. Added the `gitManager` struct + `publish()` helper to `git.go` (deferred from G2/G8 — needed a receiver type to port methods onto); still no `NewGitManager`, since 13 interface methods (`WriteFile`/`ReadFile`/`DeleteFile`/`ListDirectory`/`Log`/`Diff` — G5; `ImportRepo`/`GetImportStatus`/`CancelImport` — G5; `FetchBranch`/`GetFetchBranchStatus`/`IndexPushedBranch` — G6; `SearchBlobs` — G7) still have no body, so `*gitManager` doesn't satisfy `GitManager` yet. Ported all 9 files close to verbatim (import path + package name only); light doc-comment cleanup where text referenced dropped v1 concepts (`IterReferences`/git-smart-HTTP rationale on the branch `sha` field, `AQL-backed` → `recursive-CTE-backed` in `git_impl_graph.go`'s header, `CodeValdWork` → `mwanachama-taskmanager`). **One real deviation from the file-grouping**: `allBlobsAtCommit` + `entityToBlob` were pulled forward from what becomes G5's `fileops.go` into `git_impl_converters.go`, because G4's `edgelifecycle.go` (`replicateDocEdges`/`deleteDocEdgesForBranch`) calls `allBlobsAtCommit` and — despite living in the go-git-touching source file upstream — that one function and its converter are pure `entitygraph.DataManager` traversal with zero go-git coupling (verified by reading it). Added `fake_datamanager_test.go` (in-process `entitygraph.DataManager`, not a Postgres stand-in — just enough CRUD + BFS `TraverseGraph` to exercise the ported control flow) and `git_impl_test.go` (7 tests: repo/branch lifecycle incl. duplicate-name and default-branch-delete guards, merge advancing default HEAD, full MR lifecycle, tag CRUD, keyword tree + delete-reparents-children, CreateEdge/DeleteEdge + SearchByKeywords, and RollbackByWorkflowRun incl. idempotent re-invocation). `go build`, `go vet`, `go test`, `gofmt -l` all clean. |
| G5 | Port go-git-touching impl files (blobcache, fileops, import) — build tree/blob objects via `go-git/plumbing/object`; no wire-protocol transport | 2026-09-02 | **Scope changed mid-task, user decision.** Reading `git_impl_import.go` showed `ImportRepo` does a real network `git clone` (`gogit.PlainCloneContext` against the caller's URL) and its auto-populate step calls `FetchBranch` directly — which the board had scoped to G6 ("fetchbranch/push/index-sync… default is to skip unless confirmed needed"). Asked the user rather than picking a lane unilaterally; they chose to pull `FetchBranch`'s core logic forward so `ImportRepo` works end-to-end now, rather than stub the auto-fetch or defer all of import to G6. So this task ended up covering `git_impl_fetchbranch.go` (`FetchBranch`/`GetFetchBranchStatus`) too, on top of the three files it was named for. Added `github.com/go-git/go-git/v5` (pinned to the same `v5.16.5` the source repo uses) as a real dependency — `go mod tidy` run after. **`git_impl_blobcache.go` was adapted, not ported verbatim**: the original hydrated stub-blob content via `m.backend.OpenStorer` (the v1 `Backend` abstraction, dropped per CLAUDE.md). Since `FetchBranch`'s `deepenClone` already leaves a full local clone on disk at a path recorded on the Repository entity's `bare_clone_path` property, the rewritten `loadBlobContentFromBareClone` just opens that path directly with `gogit.PlainOpen` — simpler than the original and needs no `Backend` interface at all. This also fixed a stray doc/impl name mismatch in the source (a comment in `fetchbranch.go` already referred to a function called `loadBlobContentFromBareClone` that didn't actually exist under that name upstream). `git_impl_fileops.go` ported close to verbatim (`entityToBlob`/`allBlobsAtCommit` excluded — already pulled into `converters.go` during G4); `git_impl_index.go`/`git_impl_push.go` (real git-push wire protocol / `IndexPushedBranch`) were left for G6, since they're unrelated to `ImportRepo`'s network-clone path the user asked to unblock. **One added call not in the source**: `WriteFile` now publishes `TopicFileWritten` — the original declared this topic and listed it in `AllTopics()` but never actually called `m.publish` for it anywhere in `git_impl_*.go`; grepping showed the doc comment's "published by CodeValdGit after WriteFile" promise was fulfilled by the dropped v1 event-consumer server layer, which doesn't exist here, so the topic would otherwise be permanently unreachable dead code despite being ported forward in G8. Firing it from `WriteFile` itself matches how every other topic in this codebase is published (inline in the owning method). **Two real bugs found via testing and fixed** (present in the original source, not introduced by the port): `GetBranch`, `GetTag`, and `GetKeyword` fetched by raw entity ID without checking `TypeID`, unlike `GetRepository`/`GetMergeRequest`, which do — since `entitygraph.DataManager.GetEntity` is a single global-ID lookup with no per-type scoping (one Postgres `entities` table, by design), a Commit ID passed to `GetBranch` came back as a zero-valued `Branch` instead of `ErrBranchNotFound`. `Diff`'s `resolveRef` surfaced this concretely: a commit ID given as a ref was misread as a no-HEAD-commit branch. Added the same `TypeID` guard the other two `Get*` methods already had, plus `TestGetByIDRejectsWrongEntityType` as a regression test. Also added `git_impl_fileops_test.go` (write/read/delete/list-directory/log/diff round-trip, plus `ImportRepo`/`FetchBranch` synchronous guard-clause tests that don't touch the network — the real clone path needs G9's integration-test setup, not a unit test). `go build`, `go vet`, `go test`, `gofmt -l` all clean; only `IndexPushedBranch` and `SearchBlobs` remain unimplemented on `*gitManager`. |

## Archived board context

`mwanachama-git` — Go library, module `github.com/aosanya/mwanachama-git`.
Consumed by [mwanachama-api-gateway](../../../mwanachama-api-gateway) for
[mwanachama-kazi](../../../mwanachama-kazi).

### Why this repo exists

`mwanachama-kazi` needs git-like versioned content. `CodeValdGit` already
built this — as an ArangoDB-backed library for CodeValdCortex agencies, with
a gRPC service and (in its v1 API) real git wire-protocol serving. Neither
of those fit `mwanachama-api-gateway`, which is Postgres-only and runs as one
service with no sub-services.

**Decision: full port, same public API surface, v2 (`GitManager`) only.**
Exploration found `CodeValdGit`'s business logic (`git_impl_*.go`) is almost
entirely storage-agnostic — it calls `entitygraph.DataManager`, never AQL
directly, apart from one seam (`searcher.go`'s blob full-text search). So the
port is: reuse `git_impl_*.go`/`models.go`/`git.go` close to verbatim, retarget
storage onto [mwanachama-go-shared](../../../mwanachama-go-shared)'s Postgres
entity-graph engine, drop gRPC/proto/cmd/registrar, and drop the go-git
wire-protocol layer (v1 `Backend`, `storer.go`, `githttp.go`) since
`mwanachama-kazi` doesn't need real `git clone`/`push` interop.

Full task breakdown and rationale: originally scoped in
`/Users/tony/.claude/plans/kind-snacking-rose.md`.
