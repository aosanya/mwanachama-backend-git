# mwanachama-backend-git (Go)

Open tasks only — 🚀 In Progress · 📋 Not Started · ⏸️ Blocked.
Everything else (completed rows, board context) is in [todo_done.md](todo_done.md).

| Task | Title | Status | Depends on |
|------|-------|--------|------------|
| G6 | Real git-push wire protocol for `IndexPushedBranch` (Smart HTTP receive-pack indexing) — currently a stub returning `ErrPushIndexingNotImplemented` so `*gitManager` compiles; default is to skip unless confirmed needed | 📋 | G4 |
| G12 | 🐞 **SECURITY — `GetKeywordTree` (`git_impl_keywords.go`) has no depth or node-count budget, unlike its sibling traversal method.** Found by the 2026-09-14 scheduled security-QA sweep (primary repo `mwanachama-backend-api-kazi` this run), while checking this run's Cost/Availability brief item against every read-only `git_*`/`taskmanager_*` MCP tool `mwanachama-backend-api-kazi` exposes. **Did:** read `GetKeywordTree`/`buildKeywordTreeNode` against its closest sibling, `GetNeighborhood` (`git_impl_graph.go`) — the latter carries an explicit, doc-commented `clampDepth` (`[1,3]`) plus a hard `neighborhoodMaxNodes=100` visited-vertex cap, live-verified this run via a new self-test in `mwanachama-backend-api-kazi` (`security-03-neighborhood-depth-clamped`, depth=999999999 still answers with ≤100 nodes). **Observed:** `buildKeywordTreeNode` recurses once per child via `gormstore.KeywordChildIDs` (no `.Limit()`, no depth counter, no total-node budget at all) until the real taxonomy bottoms out — no analogous cap exists anywhere in the call path. **Threat:** a keyword taxonomy of real depth/width (via `GitManager.CreateKeyword` — not reachable through any MCP tool `mwanachama-backend-api-kazi` exposes today, since every `git_*` tool it registers is read-only, but a real method on the shared `GitManager` interface every future caller inherits, including this repo's own not-yet-wired `routes/` package) turns one `GetKeywordTree`/`git_get_keyword_tree` call into unbounded per-node query fan-out — the same resource-exhaustion shape K12 (see `mwanachama-backend-api-kazi/documentation/3. implementation/todo_done.md`) already closed for every `List*` method in this repo (`ListKeywords` itself already caps at `maxListPage=500`). **Control:** none — no depth parameter, no node-count budget anywhere in `GetKeywordTree`/`buildKeywordTreeNode`. **Verdict:** open (case B), but not currently reachable through `mwanachama-backend-api-kazi`'s own `/mcp` surface (no MCP tool there can create a `Keyword` at all), so filed **P2** (a documented invariant — the depth/node-cap discipline `GetNeighborhood` already models two functions away — unenforced, with no currently reachable path) rather than as a live P0/P1, and as a board row here rather than a self-test in `-api-kazi` (a self-test cannot demonstrate the gap without a write path to seed enough `Keyword` rows to matter). **Fix:** give `GetKeywordTree` the same `clampDepth`-shaped budget `GetNeighborhood` already carries — a max depth and a total node cap mirroring `neighborhoodMaxNodes` — before any write path (this repo's own `routes/` once wired into a gateway, or a future kazi write tool) can put real data behind it. | 📋 | — |
