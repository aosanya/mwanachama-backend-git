# mwanachama-backend-git (Go)

Open tasks only — 🚀 In Progress · 📋 Not Started · ⏸️ Blocked.
Everything else (completed rows, board context) is in [todo_done.md](todo_done.md).

| Task | Title | Status | Depends on |
|------|-------|--------|------------|
| G15 | 🐞 **SECURITY — `GetKeywordTree` walks a keyword's ENTIRE subtree with no depth cap and no node-count cap, and the `git_get_keyword_tree` MCP tool gives the caller no `depth` parameter to limit it either — one N+1-query recursive call per keyword call.** Unlike `GetNeighborhood` (`git_impl_graph.go`), which explicitly `clampDepth`s its caller-supplied depth and caps total nodes at `neighborhoodMaxNodes` (tested: `TestGetNeighborhood_DepthClamp`, `TestGetNeighborhood_NodeCapEnforced`), `GetKeywordTree`/`buildKeywordTreeNode` (`git_impl_keywords.go:141`/`:224`) recurse into every descendant unconditionally: each node issues its own `gormstore.KeywordChildIDs` query (itself an uncapped `Pluck`, no `.Limit()`) plus a row fetch, then recurses into each child the same way — one DB round trip per node in the whole subtree, and the tool's own params (`gitKeywordIDParams`: just `keyword_id`) give the caller no way to ask for less. **Did**: seeded a 20-deep keyword chain plus 50 sibling leaves under one root via the real `GitManager.CreateKeyword` (an ordinary write, no privilege needed), then called `GetKeywordTree(ctx, rootID)` — the exact operation `git_get_keyword_tree(keyword_id=rootID)` performs over MCP. **Observed**: all 70 descendant nodes returned in one call, fully hydrated, no truncation — confirmed by a throwaway test (`go test -run TestScratch_GetKeywordTree_Unbounded`, deleted before commit, not left in the tree). **Threat**: reachable via both `mwanachama-backend-api-kazi`'s `/mcp` (behind `requireCapabilityStub` — any caller) and `mwanachama-backend-api-gateway`'s `/mcp` (behind real session auth + `git.read` capability — any caller holding that one ordinary read grant, not an elevated one); a keyword taxonomy that has grown deep or wide over time (this is exactly the shape a real keyword/tag hierarchy tends to take) turns one cheap tool call into an unbounded number of sequential DB round-trips and an unbounded response payload. **Control**: none — no depth clamp, no node cap, no caller-facing depth/limit parameter at all. **Verdict**: real, reproducible gap, same class as `mwanachama-backend-taskmanager`'s `TraverseRelationships` finding (see that repo's own board, filed same sweep) but independent code and independent fix; not filed as a permanent self-test anywhere (fixing it is source-level). Fix shape: mirror `GetNeighborhood`'s own pattern — take a caller-supplied depth, clamp it, and cap total returned nodes, in both `GetKeywordTree`'s public signature and `buildKeywordTreeNode`'s recursion guard; `mwanachama-backend-api-shared`'s `mcpGitGetKeywordTree` tool params would then need a `depth` field the way `gitNeighborhoodParams` already has one. | 📋 | None — found 2026-09-21 by the scheduled cross-repo security QA sweep's cross-repo replication pass (primary this run: `mwanachama-backend-api-kazi`; this repo's own list-cap convention, `list_limit.go`'s `maxListPage`, was the lead that prompted checking every other MCP-exposed traversal for the same shape). No product code touched; the reproduction test file was deleted after confirming the result. |
